<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ClassMapper">
	<select id="select" resultType="ClassDTO" parameterType="string">
		select *
		from class
		where classid=#{classId}
	</select>
	
	<select id="selectUserName" parameterType="string" resultType="string">
		select username
		from class c join login l using(userid)
		where userid = 'user01'
	</select>
    
<!-- 	<select id="selectContent" parameterType="hashmap" resultType="string">
    	select
    	<choose>
    		<when test="${#{con}=='con_class'}">con_class</when>
    		<when test="${#{con}=='con_tutor'}">con_tutor</when>
    		<when test="${#{con}=='con_attention'}">con_attention</when>
    		<when test="${#{con}=='con_notice'}">con_notice</when>
    	</choose>
    	where classid = #{classId}
    </select>  -->
    
    <select id="selectClass" parameterType="string" resultType="string">
    	select con_class
    	from content
    	where classid = #{classId}
    </select>
</mapper>